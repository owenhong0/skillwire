# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# logs
*.log

# node / vite
node_modules/
frontend/dist/

# python / backend
.venv/
venv/
__pycache__/
*.pyc

# secrets
.env
backend/.env
frontend/.env
frontend/.env.local
# NOTE: frontend/.env.production is intentionally committed — it holds only the
# public backend URL (VITE_API_BASE), which Vite inlines into the static bundle.
